#include <stdio.h>
#include <stdlib.h>

/* There can only be 1 optimal solution
    Find it by using:
    1. Greedy Method
    2. Dynamic Programming
    3. Branch & Bound
*/
 
int Greedy(int x, int y) {
    
}

int main() {
    int coins[] = {29, 30, 50, 90};
    int x; scanf("%d", &x);
    return 0;
}